// Reusable Card UI Component Skeleton
// TODO (Assignee - Frontend Developer): Style card component according to your design preference

import * as React from "react";

export function Card({ children }: { children: React.ReactNode }) {
  return <div>{children}</div>;
}
